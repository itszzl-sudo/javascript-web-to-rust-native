[package]
name = "jrust-platform"
version = "0.1.0"
edition = "2021"
description = "Platform abstraction layer for JRust (Desktop/iOS/Android)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[features]
default = []
desktop = []
ios = []
android = []
